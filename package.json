{
	"name": "utsukushi-bot",
	"version": "2.0.0",
	"author": "DRACOX2500",
	"license": "MIT",
	"description": "Discord Bot for a private Discord Server",
	"readme": "",
	"repository": {
		"url": ""
	},
	"main": "index.ts",
	"scripts": {
		"build": "tsc index.ts",
		"deploy-commands": "echo \"Error: no deploy-commands specified\" && exit 1",
		"reset-commands": "echo \"Error: no reset-commands specified\" && exit 1",
		"start": "node dist/index.js",
		"super-start": "npm run build && deploy-commands  && npm run start",
		"nodemon": "echo \"Error: no nodemon specified\" && exit 1",
		"lint": "echo \"Error: no lint specified\" && exit 1",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"bot",
		"discord",
		"music",
		"javascript",
		"typescript"
	],
	"dependencies": {
		"discord.js": "^14.11.0",
		"dotenv": "^16.0.3"
	},
	"devDependencies": {
		"nodemon": "^2.0.22",
		"typescript": "^5.0.4"
	}
}
